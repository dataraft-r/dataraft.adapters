% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-parquet.R
\name{dr_source_parquet}
\alias{dr_source_parquet}
\title{Read Parquet files or an Arrow dataset}
\usage{
dr_source_parquet(path, lazy = TRUE, ...)
}
\arguments{
\item{path}{File, dataset directory or Arrow-supported URI.}

\item{lazy}{Return a lazy Dataset instead of an ordinary tibble.}

\item{...}{Named arguments to \code{\link[arrow:open_dataset]{arrow::open_dataset()}}.}
}
\value{
A source specification for \code{\link[dataraft.core:dr_add_source]{dataraft.core::dr_add_source()}}.
}
\description{
Arrow handles local files, directories and supported object-storage URIs.
Configure remote credentials through Arrow. A lazy source returns an Arrow
Dataset; use dplyr verbs and \code{\link[dataraft.core:dr_collect]{dataraft.core::dr_collect()}} to materialize it. Remote availability
is checked when reading, not during structural validation.
}
\examples{
\dontshow{if (requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
path <- tempfile(fileext = ".parquet")
arrow::write_parquet(data.frame(id = 1:2), path)
dataraft.core::dr_read_source(dr_source_parquet(path)) |> dataraft.core::dr_collect()
unlink(path)
\dontshow{\}) # examplesIf}
}
