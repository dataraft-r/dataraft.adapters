% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{dr_init_project}
\alias{dr_init_project}
\title{Start a runnable data-product project}
\usage{
dr_init_project(
  path,
  name = "orders",
  renv = FALSE,
  targets = FALSE,
  connect = FALSE
)
}
\arguments{
\item{path}{New or empty project directory.}

\item{name}{Product identity.}

\item{renv}{Write \code{init-renv.R} for an optional isolated R environment.}

\item{targets}{Write \verb{_targets.R} for dependency-aware execution.}

\item{connect}{Write \code{job.qmd} and deployment instructions for Posit Connect.}
}
\value{
The normalized project path, invisibly.
}
\description{
Creates a small project with synthetic CSV data, one product definition and
a runner. Optional files integrate with targets, renv or Posit Connect.
No packages are installed, services contacted or workflows executed.
Existing non-empty directories are never overwritten.
}
\details{
Run \code{source("run.R")} in the new project to create \code{output.rds} and durable
run evidence. Set \code{DATARAFT_EVIDENCE} to an appropriate persistent directory
in deployments. The renv option writes a bootstrap script; a lockfile is
created only when you explicitly run that script.
}
\examples{
path <- tempfile("orders-project-")
dr_init_project(path)
list.files(path)
unlink(path, recursive = TRUE)
}
\seealso{
\code{\link[dataraft.core:dr_product]{dataraft.core::dr_product()}}, \code{\link[=dr_as_targets]{dr_as_targets()}}
}
