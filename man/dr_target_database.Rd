% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-database.R
\name{dr_target_database}
\alias{dr_target_database}
\title{Publish a checked table through DBI}
\usage{
dr_target_database(
  connection,
  table,
  mode = c("replace", "append"),
  transaction = TRUE,
  ...
)
}
\arguments{
\item{connection}{DBI connection or zero-argument connection factory.}

\item{table}{Table name or \code{\link[DBI:Id]{DBI::Id()}}.}

\item{mode}{Replace the table or append, with a gate before commit.}

\item{transaction}{Must be \code{TRUE}. Nontransactional publication requires a
custom \code{\link[dataraft.core:dr_write_target]{dataraft.core::dr_write_target()}} adapter with an explicit consistency policy.}

\item{...}{Named additional arguments to \code{\link[DBI:dbWriteTable]{DBI::dbWriteTable()}}.}
}
\value{
A target specification for \code{\link[dataraft.core:dr_set_target]{dataraft.core::dr_set_target()}}.
}
\description{
Writing and candidate validation require a DBI transaction. Before committing,
the adapter reads the complete stored table and checks it against the contract.
This catches constraint violations caused by driver casts as well as append
conflicts with existing rows. It materializes the stored table in R. Append
requires an explicit contract; direct replacement without one infers structure
from the incoming table. \code{\link[dataraft.core:dr_run]{dataraft.core::dr_run()}} always supplies its resolved contract.
}
\details{
The driver and table storage must support transactions, including rollback of
replacement DDL. No silent fallback is provided. Coordinate concurrent writers
externally; isolation and transactional DDL remain the driver's responsibility.
Transactional SQLite and DuckDB are covered by the package tests. An arbitrary
DBI driver or nontransactional table is not certified by a successful preflight.
DuckDB connections reading or writing \code{BIGINT}/\code{integer64} columns must use
\code{bigint = "integer64"} to prevent lossy conversion. This is checked before
appending to an existing BIGINT table and before validating a stored table.
For append, \code{dataraft.core::dr_collect(result)} returns the transformed incoming batch;
\code{result$outputs$rows} counts the complete destination and \code{written_rows}
counts appended rows. Run quality evidence describes the complete candidate.

Caller connections stay open; factory connections close after success and
failure. Extra arguments go to \code{\link[DBI:dbWriteTable]{DBI::dbWriteTable()}}. Reserved arguments
\code{conn}, \code{name}, \code{value}, \code{append}, and \code{overwrite} cannot be overridden.
}
\examples{
\dontshow{if (requireNamespace("RSQLite", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
con <- DBI::dbConnect(RSQLite::SQLite(), ":memory:")
result <- dataraft.core::dr_product("orders") |>
  dataraft.core::dr_add_source(data.frame(id = 1:2)) |>
  dataraft.core::dr_set_target(dr_target_database(con, "orders")) |>
  dataraft.core::dr_run()
DBI::dbReadTable(con, "orders")
DBI::dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
