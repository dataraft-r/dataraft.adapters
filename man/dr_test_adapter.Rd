% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-adapter.R
\name{dr_test_adapter}
\alias{dr_test_adapter}
\title{Check an adapter implementation against the extension protocol}
\usage{
dr_test_adapter(
  adapter,
  expected = NULL,
  data = NULL,
  context = list(),
  read_back = NULL
)
}
\arguments{
\item{adapter}{Source or target adapter.}

\item{expected}{Optional expected source or roundtrip data frame.}

\item{data}{Optional data frame to write to a target.}

\item{context}{Target context including an explicit contract.}

\item{read_back}{Optional function accepting the write result and returning
the committed table. Required when testing a target write.}
}
\value{
Invisibly, a list with capabilities, descriptor, read and write results.
Protocol violations raise \code{dr_adapter_nonconformant}; adapter execution
errors retain their original classes.
}
\description{
Checks preflight, descriptive metadata and the six standard capabilities.
\code{NA} capability values are allowed as undeclared; they do not trigger I/O.
Source adapters are read and compared with \code{expected} when provided. Target
adapters are written only when \code{data} and an explicit \code{context$contract} are
supplied. Use a disposable target: this helper performs real I/O. A
\code{read_back} callback reads the committed result for a roundtrip comparison.
This smoke test cannot certify transactionality, concurrency, secret hygiene
or remote failure recovery. Test those guarantees separately in your adapter.
}
\examples{
dr_test_adapter(data.frame(id = 1:2), expected = data.frame(id = 1:2))
}
