% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{dr_source_database}
\alias{dr_source_database}
\title{Describe a DBI table or SQL query as a source}
\usage{
dr_source_database(
  connection,
  table = NULL,
  query = NULL,
  params = NULL,
  lazy = !is.function(connection) && is.null(params)
)
}
\arguments{
\item{connection}{DBI connection or function opening one.}

\item{table}{Table name or \code{\link[DBI:Id]{DBI::Id()}}.}

\item{query}{A single SQL query string.}

\item{params}{Parameters passed to \code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}}. Parameterized
queries are materialized, because DBI binding is not a lazy query.}

\item{lazy}{Keep a caller-owned DBI table lazy. Defaults to \code{TRUE} for open
connections without parameters and \code{FALSE} for factories. Factories are
closed before returning and cannot provide lazy output.}
}
\value{
A source specification accepted by \code{\link[dataraft.core:dr_add_source]{dataraft.core::dr_add_source()}}.
}
\description{
Supply exactly one of \code{table} and \code{query}. The connection may be an open DBI
connection or a zero-argument factory. Only factory-created connections are
closed by dataraft. SQL is passed to DBI unchanged; use \code{params} for values.
Nothing is queried during construction or structural preflight.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
con <- DBI::dbConnect(duckdb::duckdb(), bigint = "integer64")
DBI::dbWriteTable(con, "orders", data.frame(id = 1:2))
dataraft.core::dr_read_source(dr_source_database(con, table = "orders"))
DBI::dbDisconnect(con, shutdown = TRUE)
\dontshow{\}) # examplesIf}
}
