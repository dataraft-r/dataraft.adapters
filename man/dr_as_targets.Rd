% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orchestration.R
\name{dr_as_targets}
\alias{dr_as_targets}
\title{Execute product dependencies with targets}
\usage{
dr_as_targets(x, cue = NULL, evidence = NULL)
}
\arguments{
\item{x}{Table product or named list of table products. Nested products are
included. Schedule a complete model publication with targets::tar_target().}

\item{cue}{Optional \code{\link[targets:tar_cue]{targets::tar_cue()}} applied to product targets.}

\item{evidence}{Optional directory for \code{\link[dataraft.core:dr_run]{dataraft.core::dr_run()}} evidence.}
}
\value{
A list of objects from \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}. Product target
names are derived from product IDs with \code{make.names()}; conflicting names
are rejected. Collect a stored result with \code{dataraft.core::dr_collect(targets::tar_read(id))}.
}
\description{
Converts one product, or a named list of products, into ordinary targets
targets. Nested product sources become explicit upstream dependencies.
File sources get separate \code{format = "file"} targets so changes invalidate
downstream products. Constructing the graph never runs a product.
}
\details{
Place \code{dr_as_targets(...)} at the end of \verb{_targets.R}, then use
\code{targets::tar_make()}. targets owns caching, dependency scheduling and
parallelism.
Each product is run once in the graph; downstream products use its completed
output. Product IDs must identify one definition throughout the graph.

Only local file changes are inferred automatically. For APIs, databases and
other externally changing sources, provide an appropriate \code{cue}, commonly
\code{targets::tar_cue(mode = "always")}. Use connection factories; open DBI
connections and lazy tables cannot be safely stored in a targets cache.
A cached product does not create new run evidence until it executes again.
Function bodies and captured values are part of the embedded specification;
source project code from \verb{_targets.R} to rebuild definitions when it changes.
Static references to ordinary captured values are tracked. Dynamic lookups
and mutable environment state need an explicit cue, just like remote data.
Give definition variables names such as \code{orders_definition} when the target
is named \code{orders}, to avoid targets' global-object name collision warning.
}
\examples{
\dontshow{if (requireNamespace("targets", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
orders <- dataraft.core::dr_product("orders") |> dataraft.core::dr_add_source(data.frame(id = 1:2))
dr_as_targets(orders)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dr_init_project]{dr_init_project()}}, \code{\link[dataraft.core:dr_run]{dataraft.core::dr_run()}}
}
