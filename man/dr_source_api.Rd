% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-api.R
\name{dr_source_api}
\alias{dr_source_api}
\title{Read a paginated HTTP API using httr2}
\usage{
dr_source_api(
  request,
  parse = api_parse_json,
  next_request = NULL,
  max_pages = 1000L
)
}
\arguments{
\item{request}{An httr2 request or zero-argument request factory.}

\item{parse}{Function converting one response into a data frame.}

\item{next_request}{Optional function returning the next request or \code{NULL}.}

\item{max_pages}{Maximum number of pages, default 1000.}
}
\value{
A source specification for \code{\link[dataraft.core:dr_add_source]{dataraft.core::dr_add_source()}}.
}
\description{
Supply a prepared httr2 request, including authentication, retry and throttle
policies, or a zero-argument factory that builds one at execution time. When
no retry limit was configured, requests retry up to three attempts using
httr2's transient-status rules. Request details, headers, URLs and response
bodies are excluded from inspection and adapter error messages.
}
\details{
\code{parse} receives each response and must return a data frame. The default reads
a JSON array of records, or a JSON object with a \code{data} array. \code{next_request}
receives each response and returns another prepared request or \code{NULL}.
Pagination must terminate before \code{max_pages} is exceeded; truncation fails
rather than silently dropping rows. Repeated identical requests are rejected.
Each page must have the same column names; incompatible types fail binding.
Authentication and rate limiting remain httr2's responsibility.
}
\examples{
\dontshow{if (requireNamespace("httr2", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# Construction does not send a request.
api <- dr_source_api(httr2::request("https://example.org/orders"))
dataraft.core::dr_inspect(api)
\dontshow{\}) # examplesIf}
}
